import React from "react";
import styled from "styled-components";

// 회원가입/로그인 페이지읠 레이아웃을 담당하는 컴포넌트

const AuthTempleBlock = styled.div``;

const AuthTemplate = () => {
    return (
        <AuthTempleBlock>

        </AuthTempleBlock>
    );
};

export default AuthTemplate;